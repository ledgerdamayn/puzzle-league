=begin

     Simplified version of Block objects for use in grid.
     Can also represent a Board square not containing a Block, or
     if a square contains multiple blocks (falling)

=end

class Square
     def initialize
    
     end
end
